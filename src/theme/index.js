
import Typography, {fontFamilies}from './typography';
import Colors from './colors';



const theme = {
    typography: {
        ...Typography
    },
    colors: {
        ...Colors
    },
    fonts: {
        ...fontFamilies
    }
};

export default theme